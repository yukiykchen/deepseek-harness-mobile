<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Temporary destination for ACTION_IMAGE_CAPTURE; the file is deleted once the
         photo has been Base64-encoded for the image-prompt pipeline. -->
    <cache-path name="camera" path="camera/" />
    <!-- Exported transcripts handed to the share sheet as .html files. -->
    <cache-path name="exports" path="exports/" />
</paths>
